version: "3.9"
services:
  db:
    image: postgres:16
    volumes:
      - postgres-db:/var/lib/postgresql/data
    env_file:
      - ./.env
    ports:
      - 5432:5432
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
volumes:
  postgres-db:
